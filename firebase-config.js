// Firebase web config for cloud sync. Leave as null to run without sync.
// Firebase console → Project settings → General → Your apps → Web app → "SDK setup and configuration" → Config.
// These values are meant to be public; firestore.rules is what keeps your data private.
export const firebaseConfig = null;
